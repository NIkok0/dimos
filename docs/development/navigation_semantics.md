# Navigation Semantics (deprecated)

This document moved to [`nl_semantics.md`](nl_semantics.md).

The catalog file is now [`config/nl_semantics.yaml`](../../config/nl_semantics.yaml). The legacy [`config/navigation_semantics.yaml`](../../config/navigation_semantics.yaml) remains for reference; new edits should go to `nl_semantics.yaml`.
